// 5. HISTORY

// Propiedades
history.length; // Número de URLs en el historial de la página (de esa sesión)
    
// Métodos
history.back(); // Carga la URL anterior en el historial
history.forward(); // Carga la URL siguiente en el historial (funciona si hemos avanzado hacia adelante y hemos vuelto atrás)
history.go() // Carga una página concreta del historial. Ejemplo history.go(-3) iría tres páginas hacia atrás
